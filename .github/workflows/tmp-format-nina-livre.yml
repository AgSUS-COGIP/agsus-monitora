name: Temp Format Nina Livre

on:
  push:
    branches:
      - feat/nina-livre-fontes-oficiais

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: feat/nina-livre-fontes-oficiais
      - name: Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Dependencias
        run: npm install --no-audit --no-fund
      - name: Prettier
        run: npx prettier --write src/modules/arara-guide.js src/modules/nina-panel-drag.js
      - name: Commit
        run: |
          if git diff --quiet; then
            echo "Nenhuma alteracao necessaria."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/modules/arara-guide.js src/modules/nina-panel-drag.js
          git commit -m "style(nina): aplicar Prettier"
          git push origin HEAD:feat/nina-livre-fontes-oficiais
